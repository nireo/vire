apiVersion: v1
kind: Service
metadata:
  name: vire-vllm-headless
  labels:
    app.kubernetes.io/name: vire
    app.kubernetes.io/component: vllm
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: vire
    app.kubernetes.io/component: vllm
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
